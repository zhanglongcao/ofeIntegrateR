% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_ofe_trial}
\alias{simulate_ofe_trial}
\title{Simulate a synthetic OFE strip trial with a sparse point-source covariate}
\usage{
simulate_ofe_trial(
  n_row = 40,
  n_col = 20,
  n_treat = 3,
  treat_effects = c(0, 0.8, 1.6),
  point_range = 8,
  point_psill = 1.2,
  point_nugget = 0.1,
  dense_var_weight = 0.6,
  noise_sd = 0.4,
  n_point_samples = 10,
  point_obs_noise_sd = 0.3,
  seed = NULL
)
}
\arguments{
\item{n_row, n_col}{Integer grid dimensions (rows = one axis, columns =
the axis along which treatment strips run).}

\item{n_treat}{Integer number of treatment strips, applied as
contiguous, approximately equal-width column blocks.}

\item{treat_effects}{Numeric vector of length \code{n_treat} giving the true
treatment effect added to the dense response. Recycled with treatment
labels \code{LETTERS[1:n_treat]}.}

\item{point_range, point_psill, point_nugget}{Variogram parameters
(exponential model) for the spatially correlated point-source surface.}

\item{dense_var_weight}{Numeric; coefficient relating the point-source
surface to the dense response (i.e. how much of the dense response's
spatial pattern is explained by the point-source variable).}

\item{noise_sd}{Standard deviation of i.i.d. measurement noise added to
the dense response.}

\item{n_point_samples}{Integer number of sparse point-source samples to
draw from the grid.}

\item{point_obs_noise_sd}{Standard deviation of i.i.d. measurement
(e.g. lab) noise added to the observed point-source samples.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A list with components:
\describe{
\item{grid}{Data frame, one row per grid cell, with \code{row}, \code{col},
\code{treat}, \code{point_true} (true point-source surface) and
\code{dense_response} (e.g. simulated yield).}
\item{point_samples}{Data frame of \code{n_point_samples} sparse
observations with \code{row}, \code{col}, \code{point_true}, and \code{point_obs}
(true value plus measurement noise).}
\item{true_effects}{Named numeric vector of the true treatment
effects used to simulate \code{dense_response}.}
}
}
\description{
Generates a rectangular grid trial with treatment strips, a spatially
correlated "dense" response (e.g. yield), and a spatially correlated
"point-source" covariate (e.g. soil, tissue, or disease measurements)
that is only observed at a sparse set of sampled locations. Intended for
testing and demonstrating \code{\link[=krige_point_samples]{krige_point_samples()}}, \code{\link[=fit_integrated_kriged]{fit_integrated_kriged()}},
and \code{\link[=fit_integrated_joint]{fit_integrated_joint()}}.
}
\examples{
sim <- simulate_ofe_trial(n_row = 20, n_col = 10, n_point_samples = 8, seed = 1)
head(sim$grid)
sim$point_samples
sim$true_effects

}
