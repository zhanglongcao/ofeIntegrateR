% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-ofe.R
\name{ofe_control}
\alias{ofe_control}
\title{Control parameters for \code{\link[=fit_ofe]{fit_ofe()}}}
\usage{
ofe_control(
  maxit = 500L,
  tol = 1e-08,
  trace = FALSE,
  max_n = 2500L,
  start = NULL,
  se = TRUE
)
}
\arguments{
\item{maxit}{Integer; maximum iterations for each optimiser stage.}

\item{tol}{Numeric; convergence tolerance passed to \code{\link[stats:optim]{stats::optim()}}.}

\item{trace}{Logical; print the REML log-likelihood as it is optimised.}

\item{max_n}{Integer; refuse to fit more than this many observations. The
engine is dense: it forms and factorises an \code{n} by \code{n} matrix at every
iteration, so cost grows as \code{n^3}. Aggregate to a coarser lattice with
\code{\link[=grid_dense_layer]{grid_dense_layer()}} rather than raising this without thinking.}

\item{start}{Optional named numeric vector of starting values on the
natural scale, named as in the \code{varcomp} table (correlations, ranges,
relative variances). Useful when a fit has trouble converging.}

\item{se}{Logical; compute asymptotic standard errors for the variance
parameters from the observed information. Costs one numerical Hessian.}
}
\value{
A list of control settings.
}
\description{
Control parameters for \code{\link[=fit_ofe]{fit_ofe()}}
}
