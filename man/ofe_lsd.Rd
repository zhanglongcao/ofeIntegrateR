% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lsd.R
\name{ofe_lsd}
\alias{ofe_lsd}
\title{Predicted means with LSD letters}
\usage{
ofe_lsd(
  object,
  term,
  by = NULL,
  alpha = 0.05,
  adjust = "none",
  use = c("pairwise", "lsd"),
  decreasing = TRUE,
  sort = TRUE,
  level = 0.95
)
}
\arguments{
\item{object}{An \code{ofe_fit} from \code{\link[=fit_ofe]{fit_ofe()}}.}

\item{term}{Character; the factor whose means are to be lettered.}

\item{by}{Character or \code{NULL}; letter the means within each level of a
second factor, as in \code{ofe_lsd(fit, "treat", by = "zone")} for a
pseudo-environment analysis. Comparisons and letters stay inside a level;
the same letter in two zones means nothing.}

\item{alpha}{Significance level for the letters (default 0.05).}

\item{adjust}{Multiplicity adjustment for the pairwise p-values: \code{"none"}
(default), \code{"tukey"}, \code{"sidak"}, or any method taken by
\code{\link[stats:p.adjust]{stats::p.adjust()}}. Ignored when \code{use = "lsd"}.}

\item{use}{\code{"pairwise"} to judge each comparison on its own SED, or
\code{"lsd"} to apply a single average LSD to all of them (see above).}

\item{decreasing}{Logical; assign \code{"a"} to the largest mean (default). Set
\code{FALSE} where smaller is better, such as a disease score.}

\item{sort}{Logical; order the rows by mean rather than by factor level.}

\item{level}{Numeric; confidence level for the interval on each mean.}
}
\value{
A data frame with the \code{by} level where given, the \code{term} level,
\code{estimate}, \code{std.error}, \code{lower}, \code{upper} and \code{group} (the letters).
Attached as attributes: \code{lsd}, a one-row-per-group data frame of the
average SED, the LSD, the degrees of freedom and the settings used; and
\code{comparisons}, the full pairwise table behind the letters.
}
\description{
The table a trial report prints: each level's predicted mean, its standard
error, and the a/b/c letters saying which means are separable. Levels
sharing a letter are not significantly different at \code{alpha}.
}
\details{
Two ways of deciding "different" are offered, and they part company on
exactly the data this package is for.

\describe{
\item{\code{use = "pairwise"} (default)}{Each pair is judged on its own
standard error of difference. This is the right choice for a spatial
model of a strip trial: neighbouring strips are compared more precisely
than distant ones, and the SEDs genuinely differ.}
\item{\code{use = "lsd"}}{One least significant difference,
\eqn{t_{1-\alpha/2, \nu} \times \overline{SED}}, applied to every
comparison. This is the textbook LSD and what a balanced design implies;
it is also what most published tables mean by "LSD (5\%)". Quote the
value with the table.}
}

The default \code{adjust = "none"} gives unprotected LSD comparisons, which is
the convention these letters normally carry -- and which does not control
the family-wise error rate. With more than three or four treatments, say so
in the caption or pass \code{adjust = "tukey"}.
}
\examples{
sim <- simulate_ofe_trial(n_row = 20, n_col = 12, seed = 1)
fit <- fit_ofe(dense_response ~ treat, data = sim$grid)

tab <- ofe_lsd(fit, "treat")
tab
attr(tab, "lsd")

# Protected against multiplicity instead
ofe_lsd(fit, "treat", adjust = "tukey")

}
\seealso{
\code{\link[=ofe_means]{ofe_means()}} for the means and comparisons on their own, and
\code{\link[=compact_letters]{compact_letters()}} to letter comparisons from any other source.
}
