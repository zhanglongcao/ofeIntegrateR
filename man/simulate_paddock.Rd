% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-paddock.R
\name{simulate_paddock}
\alias{simulate_paddock}
\title{Simulate a paddock, its covariate layers, and a trial in it}
\usage{
simulate_paddock(
  n_row = 40L,
  n_col = 24L,
  cell_size = 10,
  yield_mean = 3,
  yield_range = 80,
  yield_sd = 0.6,
  covariates = NULL,
  covariate_cor = 0.6,
  n_zones = 1L,
  zone_props = NULL,
  treatments = NULL,
  n_rep = 4L,
  treat_effect = NULL,
  zone_effect = NULL,
  noise_sd = 0.3,
  randomise = TRUE,
  seed = NULL
)
}
\arguments{
\item{n_row, n_col}{Lattice size of the paddock.}

\item{cell_size}{Cell size in metres.}

\item{yield_mean}{Mean yield potential, in whatever unit you are working in
(t/ha, say).}

\item{yield_range}{Practical range of the yield-potential surface, in metres.
Short relative to the paddock gives a patchy field; long gives a gradient.}

\item{yield_sd}{Standard deviation of the yield-potential surface.}

\item{covariates}{Named list of covariate layers to draw. Each element is a
list which may name \code{mean}, \code{sd}, \code{range} and \code{cor} (its correlation with
yield potential). Defaults supply \code{elevation} and \code{clay}. Use \code{NULL} for
none.}

\item{covariate_cor}{Default correlation between a covariate and yield
potential, for layers that do not set their own \code{cor}.}

\item{n_zones}{Integer; number of pseudo-environments across the paddock.
\code{1} (the default) means no zone structure.}

\item{zone_props}{Optional proportions of the paddock length given to each
zone; defaults to equal bands. Real boundaries are not tidy, so unequal
proportions are the more realistic test.}

\item{treatments}{Optional treatment labels. When given, a trial is laid out
and harvested; when \code{NULL}, only the paddock and its layers are returned.}

\item{n_rep}{Replicates, passed to \code{\link[=make_trial_design]{make_trial_design()}}.}

\item{treat_effect}{Numeric vector of treatment effects, one per treatment,
in yield units. Defaults to an evenly spaced response.}

\item{zone_effect}{Numeric vector of multipliers, one per zone, scaling the
treatment effect in that zone. \code{rep(1, n_zones)} means the treatment works
equally everywhere.}

\item{noise_sd}{Standard deviation of the independent harvest noise.}

\item{randomise}{Passed to \code{\link[=make_trial_design]{make_trial_design()}}.}

\item{seed}{Optional random seed.}
}
\value{
A data frame with one row per cell: \code{row}, \code{col}, \code{x}, \code{y},
\code{yield_potential}, one column per covariate, \code{zone}, and -- when
\code{treatments} was given -- \code{treat}, \code{rep}, \code{plot} and \code{yield}. Classed as
\code{ofe_zones} so \code{plot()} maps the zones. \code{attr(, "truth")} holds the
settings and the realised component variances.
}
\description{
Generates a paddock whose truth is known, so that the rest of the package can
be checked rather than merely run: the zones it should find, the treatment
effect it should recover, and how much of the yield was never attributable to
treatment in the first place.
}
\section{What is simulated}{
A spatially correlated \strong{yield potential} surface is drawn first; it is the
paddock the grower already has. Each named \strong{covariate} is then drawn as a
mixture of that surface and its own independent structure, in the proportion
set by \code{covariate_cor} -- which is the point of them. A covariate unrelated
to yield is not worth zoning on, and one identical to it is an unrealistically
easy test; the default puts elevation and soil part-way, as they are.

Optional \strong{pseudo-environments} cut the paddock into bands across the
strips. Each band gets its own treatment response through \code{zone_effect}, so
a zone-by-treatment interaction is genuinely there to be found -- or, with
the default of one zone, genuinely is not, which is the more important case
to be able to test.

Given \code{treatments}, a trial is laid out with \code{\link[=make_trial_design]{make_trial_design()}} and
harvested: yield is potential, plus the treatment response for that cell's
zone, plus noise.
}

\section{Using it}{
The truth is attached as \code{attr(, "truth")} -- the zone breaks, the per-zone
treatment effects, the variance of each component -- so a check can be made
against what was simulated rather than against what looks plausible. That is
what separates this from a demonstration dataset.
}

\examples{
p <- simulate_paddock(n_row = 30, n_col = 20, n_zones = 2,
                      treatments = c("N0", "N60", "N120"), seed = 1)
head(p)
attr(p, "truth")$zone_effect

# The zoning functions can now be checked against a known answer
z <- partition_paddock(p, covariates = c("elevation", "clay"))
table(z$zone, p$zone)

}
\seealso{
\code{\link[=simulate_ofe_trial]{simulate_ofe_trial()}} for a bare lattice and
\code{\link[=simulate_yield_monitor]{simulate_yield_monitor()}} for the irregular shape a harvester records.
}
