% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid-dense-layer.R
\name{grid_dense_layer}
\alias{grid_dense_layer}
\title{Aggregate an irregular dense layer onto a regular lattice}
\usage{
grid_dense_layer(
  data,
  x = "x",
  y = "y",
  response,
  treat = NULL,
  cell_size = 5,
  fun = mean,
  n_min = 1L,
  keep_empty = TRUE
)
}
\arguments{
\item{data}{Data frame of dense-layer observations.}

\item{x, y}{Character; names of the projected coordinate columns in \code{data}
(metres, e.g. UTM/MGA easting and northing). Longitude/latitude in degrees
will produce meaningless cell sizes -- project first.}

\item{response}{Character; name of the column to aggregate (e.g. yield).}

\item{treat}{Character or \code{NULL}; name of a treatment column. When supplied,
each cell is assigned the treatment of the majority of its observations,
and \code{treat_purity} reports the majority share so that cells straddling a
strip boundary can be identified and removed.}

\item{cell_size}{Numeric; the length of a (square) grid cell in the units of
\code{x} and \code{y}. Choose it from the trial geometry: small enough to preserve
the spatial pattern, large enough that most cells hold several
observations.}

\item{fun}{Function used to aggregate \code{response} within a cell
(default \code{\link[=mean]{mean()}}); it is called with \code{na.rm = TRUE}.}

\item{n_min}{Integer; cells with fewer than \code{n_min} observations have their
response set to \code{NA}. Use this to discard cells supported by one or two
noisy yield-monitor pings.}

\item{keep_empty}{Logical; return the complete lattice including cells with
no observations (default \code{TRUE}).}
}
\value{
A data frame, one row per lattice cell, with columns \code{row}, \code{col}
(integer indices starting at 1), \code{x_centre}, \code{y_centre}, the aggregated
\code{response}, \code{n_obs}, and -- when \code{treat} is given -- the majority \code{treat}
and its \code{treat_purity}. The grid origin and cell size are attached as
\code{attr(, "grid")}.
}
\description{
Yield-monitor and proximal-sensor data arrive as an irregular cloud of
GPS-referenced observations along harvester or sprayer passes, not as the
rectangular lattice that a separable AR1\eqn{\times}AR1 residual structure
requires. This function is the first step of the integration pipeline: it
snaps the dense layer onto a regular grid of a chosen cell size, aggregates
the response within each cell, and returns a \strong{complete} lattice with
integer \code{row} and \code{col} indices ready for \code{\link[=fit_integrated_kriged]{fit_integrated_kriged()}} or
\code{\link[=fit_integrated_joint]{fit_integrated_joint()}}.
}
\details{
Cells containing no observations are retained with an \code{NA} response when
\code{keep_empty = TRUE} (the default). This matters: \code{asreml}'s \code{ar1():ar1()}
residual is defined over the full row-by-column lattice, so gaps must be
present as missing values rather than dropped rows.
}
\examples{
# An irregular cloud of passes across a 100 x 60 m trial
set.seed(1)
pass_y <- rep(seq(2, 58, by = 4), each = 120)
cloud <- data.frame(
  x = rep(seq(1, 99, length.out = 120), times = length(unique(pass_y))),
  y = pass_y + stats::rnorm(length(pass_y), 0, 0.3)
)
cloud$treat <- cut(cloud$x, breaks = c(0, 33, 66, 100), labels = c("A", "B", "C"))
cloud$yield <- as.numeric(cloud$treat) + stats::rnorm(nrow(cloud), 0, 0.5)

g <- grid_dense_layer(cloud, response = "yield", treat = "treat", cell_size = 5)
head(g)
table(is.na(g$yield))

}
