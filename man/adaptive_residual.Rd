% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/partition-pe.R
\name{adaptive_residual}
\alias{adaptive_residual}
\title{Build a residual structure that matches the realised geometry}
\usage{
adaptive_residual(data, zone = "zone", row = "row", col = "col")
}
\arguments{
\item{data}{Data frame containing the zone and position columns.}

\item{zone}{Character; the pseudo-environment column, e.g. from
\code{\link[=partition_pseudo_env]{partition_pseudo_env()}}.}

\item{row, col}{Character; the two lattice dimensions.}
}
\value{
A one-sided formula, with the per-zone geometry attached as
\code{attr(, "geometry")} and the number of zones that had to be demoted to
\code{id()} as \code{attr(, "n_degenerate")}.
}
\description{
Writes the \code{dsum()} residual formula for a pseudo-environment analysis, one
independent section per zone. The reason this needs a function rather than a
literal formula is that AR1 needs at least two levels in a dimension, and
real zones are not guaranteed to have them: a narrow zone may be one row
deep, and asking for \code{ar1()} there gives an unidentifiable parameter and a
failed fit. Each section therefore gets \code{ar1()} only in the dimensions where
it actually has extent, and \code{id()} elsewhere.
}
\details{
The formula is plain text that both \code{\link[=fit_ofe]{fit_ofe()}} and \code{asreml::asreml()}
accept, so the same analysis script runs with or without a licence.
}
\examples{
sim <- simulate_ofe_trial(n_row = 40, n_col = 12, point_range = 6, seed = 3)
z <- partition_pseudo_env(sim$grid, response = "dense_response",
                          along = "row", treat = "treat")
r <- adaptive_residual(z)
r
attr(r, "geometry")

}
