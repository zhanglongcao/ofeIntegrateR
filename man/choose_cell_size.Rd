% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose-cell-size.R
\name{choose_cell_size}
\alias{choose_cell_size}
\title{Choose a cell size for gridding}
\usage{
choose_cell_size(
  data,
  response,
  x = "x",
  y = "y",
  treat = NULL,
  sizes = NULL,
  min_obs = 3,
  min_purity = 0.7
)
}
\arguments{
\item{data}{Data frame of dense-layer observations.}

\item{response}{Character; the column to be aggregated.}

\item{x, y}{Character; projected coordinate columns, in metres.}

\item{treat}{Character or \code{NULL}; a treatment column. Without it, purity
cannot be assessed and only the support columns are reported.}

\item{sizes}{Numeric vector of cell sizes to try. Defaults to a spread from
roughly the typical spacing between observations up to twenty times it.}

\item{min_obs}{Numeric; the median observations per occupied cell a size
must reach.}

\item{min_purity}{Numeric; the share of occupied cells that must lie wholly
within one treatment.}
}
\value{
A data frame with one row per size: \code{cell_size}, \code{cells} in the
lattice, \code{occupied} cells, \code{coverage} (the share occupied),
\code{median_obs} per occupied cell, \code{pure} (share of occupied cells with
\code{treat_purity == 1}), and \code{usable} (occupied, supported and pure). The
recommendation and the rule behind it are attached as
\code{attr(, "recommended")} and \code{attr(, "rule")}.
}
\description{
Evaluates a range of cell sizes for \code{\link[=grid_dense_layer]{grid_dense_layer()}} and reports the
trade-off each one makes, because there is no default that survives contact
with a real trial: the right size depends on how fast the monitor logged, how
wide the plots are, and how the passes line up with the treatment boundaries.
}
\details{
Two things pull in opposite directions. Small cells keep spatial detail but
rest on few observations each, so cell means are noisy and more cells
straddle a treatment boundary and have to be thrown away. Large cells are
well supported and mostly pure, but average away the variation the spatial
model exists to capture, and eventually average across the treatments
themselves.

The default recommendation is the \strong{smallest} size meeting both floors --
enough observations per cell, and enough cells lying inside a single
treatment. Smallest, because detail lost to a coarse grid cannot be
recovered, while the floors are what keep the cells trustworthy. It is a
stated rule, not a fact: the table is returned so you can apply a different
one.
}
\examples{
sim <- simulate_yield_monitor(seed = 11)
tab <- choose_cell_size(sim$cloud, response = "yield", treat = "treat")
tab
attr(tab, "recommended")

}
\seealso{
\code{\link[=grid_dense_layer]{grid_dense_layer()}}, and \code{\link[=clean_yield_monitor]{clean_yield_monitor()}} to run first --
deciding a cell size from a cloud that still contains pass-end artefacts
sizes the grid around noise.
}
