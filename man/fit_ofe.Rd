% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-ofe.R
\name{fit_ofe}
\alias{fit_ofe}
\title{Fit a spatial mixed model without asreml}
\usage{
fit_ofe(
  fixed,
  data,
  random = NULL,
  residual = ~ar1(row):ar1(col),
  control = ofe_control()
)
}
\arguments{
\item{fixed}{Two-sided formula for the fixed effects, e.g.
\code{yield ~ treat} or \code{yield ~ zone + zone:treat}.}

\item{data}{Data frame holding every variable used by \code{fixed}, \code{random} and
\code{residual}. Rows with missing values in any of them are dropped.}

\item{random}{Optional one-sided formula of random effects, e.g. \code{~ rep} or
\code{~ rep + rep:strip}. Each term contributes one variance component; all
variables in a term are treated as factors.}

\item{residual}{One-sided formula giving the residual structure (see
above). Defaults to \code{~ ar1(row):ar1(col)}.}

\item{control}{A list from \code{\link[=ofe_control]{ofe_control()}}.}
}
\value{
An object of class \code{ofe_fit}: a list with elements \code{coefficients},
\code{vcov}, \code{sigma2}, \code{varcomp}, \code{loglik}, \code{fitted}, \code{residuals}, \code{n}, \code{p},
\code{converged}, and the model frame pieces needed by the methods. See
\code{\link[=summary.ofe_fit]{summary.ofe_fit()}}, \code{\link[=wald_tests]{wald_tests()}} and \code{\link[=ofe_means]{ofe_means()}}.
}
\description{
A residual maximum likelihood (REML) fit of a linear mixed model with an
ASReml-style separable residual structure, written in base R. It exists so
that the analysis at the end of the OFE pipeline -- a treatment model with
an \code{ar1(row):ar1(col)} residual -- can be run by a collaborator who has no
ASReml-R licence, using the same formula spelling.
}
\section{Residual structures}{
\code{residual} accepts the asreml spellings, combined with \code{:} for a separable
(Kronecker) structure:

\describe{
\item{\code{id(f)}}{Independent; a bare variable name means the same thing.}
\item{\code{ar1(f)}}{First-order autoregressive across the ordered, equally
spaced levels of \code{f}. A factor uses its level order; a numeric column is
read as a position on a lattice whose spacing is the commonest gap
between its values, so a row missing from the data still counts as a
lag rather than being closed up.}
\item{\code{exp(x)}}{Exponential correlation in a numeric coordinate \code{x},
\eqn{\rho = \exp(-d/\phi)}. Use it when positions are irregular.}
\item{\code{diag(f)}}{Heterogeneous variance across the levels of \code{f}, with the
first level as the reference.}
\item{\code{dsum(~ struct | s, levels = )}}{Independent sections: each named
level of \code{s} gets its own copy of \code{struct} with its own parameters and
its own variance. This is how a pseudo-environment analysis is written;
\code{\link[=adaptive_residual]{adaptive_residual()}} builds the formula for you.}
}

Unlike asreml, cells with a missing response are simply dropped: the
correlation is evaluated from the row and column positions of the
observations that remain, so an incomplete lattice needs no padding.
}

\section{What it is not}{
This is a compact, dense implementation intended for OFE-sized problems
(a few thousand lattice cells). It does not implement the sparse average
information algorithm, \code{us()}/\code{fa()} structures, or multi-trait models. For
those, use asreml where licensed; \code{\link[=fit_integrated_kriged]{fit_integrated_kriged()}} will dispatch to
it with the same arguments.
}

\examples{
sim <- simulate_ofe_trial(n_row = 16, n_col = 8, seed = 1)
fit <- fit_ofe(dense_response ~ treat, data = sim$grid,
               residual = ~ ar1(row):ar1(col))
summary(fit)
wald_tests(fit)
ofe_means(fit, "treat")

}
\seealso{
\code{\link[=adaptive_residual]{adaptive_residual()}} to build a \code{dsum()} formula from the realised
geometry, \code{\link[=partition_pseudo_env]{partition_pseudo_env()}} to derive the sections in the first
place, and \code{\link[=fit_integrated_kriged]{fit_integrated_kriged()}} for the integration wrapper.
}
