% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/place-samples.R
\name{place_point_samples}
\alias{place_point_samples}
\title{Choose where to put point samples}
\usage{
place_point_samples(
  grid,
  n,
  design = c("random", "grid", "stratified", "nested"),
  coords = c("col", "row"),
  strata = NULL,
  spacings = NULL
)
}
\arguments{
\item{grid}{Data frame of candidate locations, typically the output of
\code{\link[=grid_dense_layer]{grid_dense_layer()}} or the \code{grid} element of \code{\link[=simulate_ofe_trial]{simulate_ofe_trial()}}.}

\item{n}{Integer; number of samples to place.}

\item{design}{One of:
\describe{
\item{\code{"random"}}{simple random sample of grid cells.}
\item{\code{"grid"}}{systematic lattice spanning the trial, spaced to respect
its aspect ratio.}
\item{\code{"stratified"}}{one random cell per stratum, strata formed by
crossing \code{strata} (if given) with blocks along the long axis. Spatially
balanced and executable without a lattice survey.}
\item{\code{"nested"}}{clustered stages at geometrically increasing spacings
around a few centres, for variogram reconnaissance rather than
interpolation.}
}}

\item{coords}{Character vector of length 2 naming the coordinate columns of
\code{grid} (defaults to the \code{col}/\code{row} lattice indices this package uses).}

\item{strata}{Optional character; a column of \code{grid} (e.g. the treatment)
whose levels should each be represented under \code{design = "stratified"}.}

\item{spacings}{Numeric vector of stage spacings for \code{design = "nested"}, in
the units of \code{coords}. Defaults to \code{NULL}, which derives three stages from
the trial extent so the design works at any scale; supply your own when the
lags of interest are known from a prior variogram.}
}
\value{
The selected rows of \code{grid}, with the design recorded in
\code{attr(, "design")}.
}
\description{
Picks \code{n} locations from a trial grid under a named sampling design. The
number of samples fixes the cost, so this function answers the complementary
question: given that budget, where should the cores go?
}
\details{
Simulation work for the AAGI-CU-RD-OFE project found that spreading samples
over the trial beats both simple random placement and clustered placement
when the samples are there to build a covariate surface, though the effect is
second-order next to the sample count. Clustered \code{"nested"} designs
concentrate pairs at short lags, which is what identifies a variogram range;
they are a reconnaissance tool and are a poor choice for interpolation.
}
\examples{
sim <- simulate_ofe_trial(n_row = 20, n_col = 12, n_point_samples = 5, seed = 1)

set.seed(1)
pts_random <- place_point_samples(sim$grid, n = 12, design = "random")
pts_grid   <- place_point_samples(sim$grid, n = 12, design = "grid")
pts_strat  <- place_point_samples(sim$grid, n = 12, design = "stratified",
                                  strata = "treat")
nrow(pts_grid)
table(pts_strat$treat)

}
