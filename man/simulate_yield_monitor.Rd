% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-yield-monitor.R
\name{simulate_yield_monitor}
\alias{simulate_yield_monitor}
\title{Simulate a trial as a yield monitor actually records it}
\usage{
simulate_yield_monitor(
  field_x = 240,
  field_y = 108,
  swath = 9,
  along_spacing = 1.5,
  band_swaths = 2L,
  treat_effects = c(0, 0.8, 1.6),
  point_range = 25,
  point_psill = 1.2,
  point_nugget = 0.1,
  dense_var_weight = 0.6,
  noise_sd = 0.4,
  gps_jitter = 0.5,
  skip_pass_prob = 0.08,
  cut_corner = TRUE,
  n_point_samples = 30L,
  point_obs_noise_sd = 0.3,
  point_design = c("random", "strip_ends"),
  defects = FALSE,
  truth_res = 3,
  seed = NULL
)
}
\arguments{
\item{field_x, field_y}{Paddock extent, in metres.}

\item{swath}{Harvester swath width (m); passes run along the \code{x} axis.}

\item{along_spacing}{Distance between recorded points within a pass (m).}

\item{band_swaths}{Number of adjacent swaths per treatment strip.}

\item{treat_effects}{Named or unnamed numeric vector of true treatment
effects; treatments are labelled \code{A}, \code{B}, ... in order.}

\item{point_range, point_psill, point_nugget}{Exponential variogram parameters
for the point-source surface, in metres.}

\item{dense_var_weight}{Coefficient relating the point-source surface to the
dense response.}

\item{noise_sd}{Standard deviation of measurement noise on the dense response.}

\item{gps_jitter}{Standard deviation of across-track position error (m).}

\item{skip_pass_prob}{Probability that a pass is missing entirely.}

\item{cut_corner}{Clip a triangle off one corner, so the paddock is not a
rectangle.}

\item{n_point_samples}{Number of point-source samples to draw.}

\item{point_obs_noise_sd}{Measurement noise on the point samples.}

\item{point_design}{\code{"random"} spreads samples over the trial;
\code{"strip_ends"} places them at both ends of every strip, the pattern growers
commonly use when each sample is expensive to collect.}

\item{defects}{Logical or a named list; add the faults a real monitor file
carries, and label each damaged point in a \code{defect} column so a cleaning
rule can be scored rather than eyeballed. \code{FALSE} (default) leaves the
cloud sound. \code{TRUE} uses settings in the range reported for commercial
monitors: the opening and closing 12 m of each pass read low and high as
grain reaches the sensor (\code{ramp_m}, \code{ramp_low}, \code{ramp_high}), the machine
slows into each turn (\code{slow_m}, \code{slow_factor}), and 2\% of readings are
dropouts or spikes (\code{outlier_prob}, \code{spike_factor}). A named list
overrides any of those.}

\item{truth_res}{Resolution (m) of the grid on which the point-source surface
is simulated before being read off at the recorded locations.}

\item{seed}{Optional integer seed.}
}
\value{
A list with \code{cloud} (the irregular dense observations, with \code{x}, \code{y},
\code{treat}, \code{point_true} and \code{yield}), \code{point_samples} (sparse observations at
arbitrary coordinates) and \code{true_effects}.
}
\description{
\code{\link[=simulate_ofe_trial]{simulate_ofe_trial()}} produces a tidy lattice: one observation per cell,
treatments aligned to whole columns. Real dense layers look nothing like
that, and code that only ever sees the tidy version tends to break on first
contact with a harvester file. This function generates the awkward version
--- GPS-referenced points along machinery passes, position error, a paddock
that is not a rectangle, occasional missing passes, and point samples taken
wherever the sampler could reach --- so a workflow can be tested end to end
through \code{\link[=grid_dense_layer]{grid_dense_layer()}}.
}
\details{
Treatment strips run along the direction of travel, as they do in practice,
each covering \code{band_swaths} adjacent passes.
}
\examples{
sim <- simulate_yield_monitor(n_point_samples = 20, seed = 2)
nrow(sim$cloud)

# The whole point: this needs gridding before it can be modelled
g <- grid_dense_layer(sim$cloud, response = "yield", treat = "treat",
                      cell_size = 9)
table(empty = g$n_obs == 0)

}
