% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv-krige.R
\name{cv_krige_surface}
\alias{cv_krige_surface}
\title{Cross-validate a kriged point-source surface}
\usage{
cv_krige_surface(
  point_data,
  value,
  coords = c("col", "row"),
  vgm_model = NULL,
  nmax = 30
)
}
\arguments{
\item{point_data}{Data frame of point-source observations.}

\item{value}{Character; name of the observed value column.}

\item{coords}{Character vector of length 2 naming the coordinate columns.}

\item{vgm_model}{Optional variogram model from \code{\link[gstat:vgm]{gstat::vgm()}}. By default the
same exponential model \code{\link[=krige_point_samples]{krige_point_samples()}} would fit is used, so the
diagnostic matches the surface actually entering the analysis.}

\item{nmax}{Maximum neighbours per prediction, passed to \code{\link[gstat:krige.cv]{gstat::krige.cv()}}.}
}
\value{
A list with \code{rmse} (root mean squared prediction error), \code{r2}
(proportion of variance explained, which is negative when kriging predicts
worse than the sample mean), \code{n} (number of points) and \code{residuals}.
The variogram used is attached as \code{attr(, "variogram")}.
}
\description{
Leave-one-out cross-validation of the point-source layer: each sample is
dropped in turn, predicted from the rest, and compared with its observed
value. It answers whether the samples are dense enough, and well enough
behaved, to reconstruct their own surface.
}
\details{
Treat this as a \strong{sampling} diagnostic, not a verdict on an analysis. In the
simulation work behind this package a better-validated surface delivered a
larger average benefit from integration, but the probability of benefiting on
any individual trial barely moved. A low or negative \code{r2} is a reason to
collect more samples next season; it does not by itself condemn the current
model. Note also that cross-validation measures whether the surface is
\emph{well estimated}, not whether it is \emph{relevant to yield} — a soil property can
be mapped perfectly and still explain nothing about the treatment response.
}
\examples{
sim <- simulate_ofe_trial(n_row = 20, n_col = 12, n_point_samples = 25, seed = 4)
cv <- cv_krige_surface(sim$point_samples, value = "point_obs")
cv$rmse
cv$r2

}
