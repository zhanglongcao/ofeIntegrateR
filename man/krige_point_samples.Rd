% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krige.R
\name{krige_point_samples}
\alias{krige_point_samples}
\title{Krige sparse point-source samples onto a target grid}
\usage{
krige_point_samples(
  point_data,
  newdata,
  value,
  coords = c("col", "row"),
  vgm_start = NULL,
  nmax = 30
)
}
\arguments{
\item{point_data}{Data frame of sparse point-source observations,
containing the coordinate columns and the value column.}

\item{newdata}{Data frame giving the target locations to predict onto
(e.g. every cell of the trial grid), containing the same coordinate
columns as \code{point_data}.}

\item{value}{Character; name of the column in \code{point_data} holding the
observed point-source value.}

\item{coords}{Character vector of length 2 giving the coordinate column
names present in both \code{point_data} and \code{newdata}.}

\item{vgm_start}{A variogram model object from \code{\link[gstat:vgm]{gstat::vgm()}} giving
starting values for \code{\link[gstat:fit.variogram]{gstat::fit.variogram()}}. Defaults to an
exponential model with psill = 1, range = \verb{max(spatial extent) / 4},
nugget = 0.1.}

\item{nmax}{Maximum number of nearest observations used for each
kriging prediction (passed to \code{\link[gstat:krige]{gstat::krige()}}).}
}
\value{
\code{newdata} with two columns appended: \verb{<value>_kriged} (the
kriging prediction) and \verb{<value>_kriged_var} (the kriging variance).
The fitted variogram model is attached as \code{attr(., "variogram")}.
}
\description{
Fits an exponential variogram to sparse point-source observations (e.g.
soil cores, tissue samples) and uses ordinary kriging to predict values
across a target grid (e.g. the full trial extent or a dense covariate
grid). This is the first step of the "kriged-covariate" integration
strategy implemented in \code{\link[=fit_integrated_kriged]{fit_integrated_kriged()}}.
}
\examples{
sim <- simulate_ofe_trial(n_row = 20, n_col = 10, n_point_samples = 12, seed = 1)
krieged <- krige_point_samples(sim$point_samples, sim$grid, value = "point_obs")
head(krieged)
attr(krieged, "variogram")

}
