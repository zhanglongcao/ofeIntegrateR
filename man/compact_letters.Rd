% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lsd.R
\name{compact_letters}
\alias{compact_letters}
\title{Compact letter display from any set of pairwise comparisons}
\usage{
compact_letters(
  comparisons,
  alpha = 0.05,
  means = NULL,
  decreasing = TRUE,
  p_col = "p.value"
)
}
\arguments{
\item{comparisons}{Either a symmetric matrix of p-values whose dimnames are
the level names, or a data frame with a p-value column and the two levels
of each comparison. The level columns are taken from \code{level1} and \code{level2}
if present, otherwise from a \code{contrast} column of the form \code{"B - A"},
otherwise from the first two non-numeric columns.}

\item{alpha}{Significance level; comparisons with \code{p.value > alpha} are
treated as "not different" and so share a letter.}

\item{means}{Optional named numeric vector of the means, used to order the
levels. Without it the levels keep the order they appear in.}

\item{decreasing}{Logical; with \code{means} supplied, order from the largest
mean down, so \code{"a"} goes to the highest. Set \code{FALSE} if smaller is better.}

\item{p_col}{Character; name of the p-value column when \code{comparisons} is a
data frame.}
}
\value{
A named character vector of letter groups, one per level, in the
order the letters were assigned.
}
\description{
Turns pairwise p-values into the a/b/c annotation: two levels share a letter
when they are not significantly different. Levels are ordered by their means
where these are supplied, so \code{"a"} marks the best treatment.
}
\details{
This is deliberately separate from \code{\link[=ofe_lsd]{ofe_lsd()}} and takes only the
comparisons, so it can letter the output of anything -- \code{asreml::predict()},
\code{emmeans}, a table typed in by hand -- and not only a fit from this package.
}
\examples{
cmp <- data.frame(
  level1 = c("A", "A", "B"),
  level2 = c("B", "C", "C"),
  p.value = c(0.40, 0.001, 0.002)
)
compact_letters(cmp, means = c(A = 3.0, B = 3.1, C = 5.0))

}
\seealso{
\code{\link[=ofe_lsd]{ofe_lsd()}} to go straight from a fitted model to a lettered table.
}
