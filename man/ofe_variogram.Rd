% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variogram.R
\name{ofe_variogram}
\alias{ofe_variogram}
\title{Empirical and fitted variogram of a point-sampled variable}
\usage{
ofe_variogram(
  data,
  value,
  x = NULL,
  y = NULL,
  trend = NULL,
  n_bin = 15L,
  cutoff = NULL,
  model = c("exponential", "spherical", "gaussian")
)
}
\arguments{
\item{data}{Data frame of point samples.}

\item{value}{Character; the column to compute the variogram of.}

\item{x, y}{Character; projected coordinate columns, in metres. Defaults to
\code{"x"}/\code{"y"}, or \code{"x_centre"}/\code{"y_centre"} when those are present.}

\item{trend}{Optional one-sided formula of terms to remove before computing
the variogram, e.g. \code{~ treat} or \code{~ treat + rep}.}

\item{n_bin}{Integer; number of distance bins.}

\item{cutoff}{Numeric; largest lag to use. Defaults to a third of the
greatest distance between samples, the usual convention -- beyond that the
bins hold few pairs and say more about the paddock's shape than its soil.}

\item{model}{Variogram model(s) to fit: any of \code{"exponential"},
\code{"spherical"}, \code{"gaussian"}. With more than one, the best weighted fit is
chosen and the others are kept for comparison.}
}
\value{
An object of class \code{ofe_variogram}: a list with \code{empirical} (a data
frame of \code{h}, \code{gamma}, \code{n_pair}), the fitted \code{model}, \code{nugget}, \code{psill},
\code{range}, \code{practical_range}, \code{sill}, \code{nugget_ratio}, \code{range_identified}
(whether the practical range falls inside the distances actually sampled),
the \code{fits} for every model tried, and the settings used. \code{\link[=plot.ofe_variogram]{plot.ofe_variogram()}} draws it;
\code{\link[=kriging_sample_interval]{kriging_sample_interval()}} takes it directly to turn it into a sampling
interval and a core count.
}
\description{
Bins every pair of samples by the distance between them, averages the
squared difference within each bin, and fits a variogram model to the
result. This is the summary that decides the rest of a sampling programme:
the range says how far one core speaks for, and the nugget says how much of
the variation no sampling density will ever resolve.
}
\section{Reading the output}{
The \strong{nugget ratio} -- nugget over total sill -- is the number to look at
first, and \code{\link[=print.ofe_variogram]{print.ofe_variogram()}} classifies it on the usual convention
(Cambardella et al. 1994): below 0.25 is strong spatial dependence, 0.25 to
0.75 moderate, above 0.75 weak. A weak-dependence variable is effectively
noise at the scale sampled, and no amount of kriging will make a useful
surface of it; that is a finding about the sampling design, and the honest
response is to sample closer together or give up on that layer, not to krige
it anyway.
}

\section{Removing a trend first}{
A variogram assumes the mean is constant. If it is not -- the treatment
raises yield on half the strips, the paddock slopes -- the variogram keeps
climbing and never reaches a sill, and the fitted range is inflated by the
trend rather than describing the correlation. Pass \code{trend} to remove that
first: \code{trend = ~ treat} fits and strips the treatment effect, and the
variogram is computed on what remains.
}

\examples{
sim <- simulate_ofe_trial(n_row = 30, n_col = 20, n_point_samples = 60,
                          point_range = 6, seed = 1)
pts <- sim$point_samples
pts$x <- pts$col
pts$y <- pts$row
v <- ofe_variogram(pts, value = "point_obs")
v
plot(v)

}
\seealso{
\code{\link[=kriging_sample_interval]{kriging_sample_interval()}}, \code{\link[=krige_point_samples]{krige_point_samples()}},
\code{\link[=cv_krige_surface]{cv_krige_surface()}}.
}
