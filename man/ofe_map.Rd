% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-map.R
\name{ofe_map}
\alias{ofe_map}
\title{Map a variable over the trial}
\usage{
ofe_map(
  data,
  value,
  x = NULL,
  y = NULL,
  type = c("auto", "sequential", "diverging", "categorical"),
  main = NULL,
  xlab = NULL,
  ylab = NULL,
  legend = TRUE,
  palette = NULL,
  cell = NULL,
  na_col = "#eeeeec",
  border = NA,
  overlay = NULL,
  ...
)
}
\arguments{
\item{data}{Data frame with one row per cell.}

\item{value}{Character; the column to colour by.}

\item{x, y}{Character; coordinate columns. Defaults to \code{"x"}/\code{"y"}, then
\code{"x_centre"}/\code{"y_centre"}, then \code{"col"}/\code{"row"}.}

\item{type}{Colour scale to use. \code{"auto"} picks categorical for a factor and
sequential otherwise; it never picks diverging, because a variable holding
negative values is not thereby a signed one, and a two-hue scale on a
variable with no meaningful zero invents a division that is not in the
data. Ask for \code{"diverging"} when zero really is the reference.}

\item{main, xlab, ylab}{Labels. \code{main} defaults to the column name.}

\item{legend}{Logical; draw the colour key.}

\item{palette}{Optional character vector of colours overriding the default
scale.}

\item{cell}{Numeric; cell size in coordinate units. Defaults to the spacing
inferred from the coordinates.}

\item{na_col}{Colour for cells whose value is missing.}

\item{border}{Colour for cell borders, or \code{NA} for none.}

\item{overlay}{Optional function of no arguments, called once the cells are
drawn and while the map panel is still the active coordinate system, to
add labels, outlines or points. Drawing after \code{ofe_map()} has returned
will not work: the legend key is a second panel, so the device's
coordinates no longer belong to the map.}

\item{...}{Passed to \code{\link[graphics:plot.default]{graphics::plot()}}.}
}
\value{
The breaks and colours used, invisibly.
}
\description{
Draws one cell per row of \code{data}, coloured by \code{value}. It is the workhorse
behind the package's other plots and is exported because most of what a
trial team wants to look at is a map of something: the yield layer, a kriged
soil surface, an elevation covariate, the zones, the residuals of a fit.
}
\details{
The colour scale follows the job the variable does, which is why \code{type}
exists and why \code{"auto"} guesses rather than always using one ramp:

\describe{
\item{\code{"categorical"}}{A factor or character column: identity, so distinct
hues, in a fixed order.}
\item{\code{"sequential"}}{A numeric column that is all one sign: magnitude, so
one hue running light to dark.}
\item{\code{"diverging"}}{Polarity, so two hues meeting at a neutral grey. Only
for variables where zero is the reference -- residuals, differences,
departures from a target. The scale is forced symmetric about zero so
that equal departures either side get equal ink; an asymmetric diverging
scale makes one direction look larger than it is.}
}
}
\examples{
sim <- simulate_ofe_trial(n_row = 24, n_col = 12, seed = 1)
ofe_map(sim$grid, "dense_response")
ofe_map(sim$grid, "treat")

}
