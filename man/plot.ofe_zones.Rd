% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-methods.R
\name{plot.ofe_zones}
\alias{plot.ofe_zones}
\title{Draw a zone map}
\usage{
\method{plot}{ofe_zones}(x, zone = "zone", label = TRUE, main = NULL, ...)
}
\arguments{
\item{x}{An \code{ofe_zones} object from \code{\link[=partition_paddock]{partition_paddock()}} or
\code{\link[=partition_pseudo_env]{partition_pseudo_env()}}.}

\item{zone}{Character; the zone column, if it was not named \code{"zone"}.}

\item{label}{Logical; write the zone number on each zone.}

\item{main}{Plot title.}

\item{...}{Passed to \code{\link[=ofe_map]{ofe_map()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
The zones from \code{\link[=partition_paddock]{partition_paddock()}} or \code{\link[=partition_pseudo_env]{partition_pseudo_env()}}, as a map.
This is the check that matters on a partition: whether the zones are shapes
you could actually work, and whether the boundaries fall where the paddock
changes or somewhere arbitrary.
}
\details{
Zones are ordered, so they are drawn on one hue running light to dark rather
than in unrelated colours, and each carries its number, so identity never
rests on the fill -- which matters past seven zones, where the steps stop
being separable.
}
\examples{
g <- expand.grid(row = 1:24, col = 1:18)
g$x <- g$col * 10
g$y <- g$row * 10
set.seed(1)
g$elevation <- 100 + 6 * exp(-((g$y - 120)^2) / 2000) + rnorm(nrow(g), 0, .2)
plot(partition_paddock(g, covariates = "elevation"))

}
