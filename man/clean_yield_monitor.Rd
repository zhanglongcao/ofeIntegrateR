% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean-yield-monitor.R
\name{clean_yield_monitor}
\alias{clean_yield_monitor}
\title{Clean a raw yield-monitor point cloud}
\usage{
clean_yield_monitor(
  data,
  yield = "yield",
  x = "x",
  y = "y",
  pass = NULL,
  order = NULL,
  speed = NULL,
  yield_range = NULL,
  min_yield = -Inf,
  outlier_mad = 4,
  local_mad = NULL,
  local_cell = NULL,
  speed_mad = 3,
  pass_trim = 0,
  edge_buffer = 0,
  drop = TRUE
)
}
\arguments{
\item{data}{Data frame of yield-monitor observations.}

\item{yield, x, y}{Character; the yield and projected coordinate columns.}

\item{pass}{Character or \code{NULL}; a pass or swath identifier. When absent it
is derived -- from \code{order} if given, otherwise from the geometry, since
passes are parallel.}

\item{order}{Character or \code{NULL}; the recording order (a time stamp or a
sequence number). Used to derive passes and to find the ends of each.}

\item{speed}{Character or \code{NULL}; a ground-speed column.}

\item{yield_range}{Numeric of length 2, or \code{NULL}; plausible yield bounds.}

\item{min_yield}{Numeric; yields at or below this are treated as sensor
dropouts. Off by default (\code{-Inf}) because the right value depends on the
units; set it to 0 for a yield in physical units.}

\item{outlier_mad}{Numeric; MADs from the median beyond which a yield is
dropped. \code{Inf} turns the rule off.}

\item{local_mad}{Numeric or \code{NULL}; deviations from the local median beyond
which a yield is dropped. \code{NULL} (default) turns the rule off.}

\item{local_cell}{Numeric or \code{NULL}; the neighbourhood size in metres for
\code{local_mad}. Defaults to a twentieth of the field's longer side.}

\item{speed_mad}{Numeric; the same for speed, when a speed column is given.}

\item{pass_trim}{Numeric; metres to drop from each end of every pass.}

\item{edge_buffer}{Numeric; metres to drop in from the field boundary.}

\item{drop}{Logical; return only the surviving rows (default), or every row
with the verdict attached.}
}
\value{
With \code{drop = TRUE}, \code{data} with the removed rows gone. With
\code{drop = FALSE}, every row, plus \code{.keep} and \code{.reason} columns -- feed that
to \code{\link[=ofe_map]{ofe_map()}} with \code{value = ".reason"} to see what was taken and from
where. Either way a per-rule tally is attached as \code{attr(, "cleaning")}.
}
\description{
Removes the points a harvester records but did not really measure, before
they reach \code{\link[=grid_dense_layer]{grid_dense_layer()}} and become part of the spatial model.
}
\section{How well it works}{
Scored against \code{\link[=simulate_yield_monitor]{simulate_yield_monitor()}} with \code{defects = TRUE}, which
labels every point it damages, using \code{pass_trim = 12}, a speed column, and
\code{local_mad = 3}: 97\% of the depressed pass starts and 90\% of the inflated
pass ends are removed, for 1\% of the sound points. On a cloud in physical
units, where \code{min_yield = 0} can be set, 94\% of the injected outliers go as
well. Without that rule only 39\% of them do, because a dropout reading of
zero is not far enough from the median to trip a dispersion threshold.
}

\section{Why it cannot be left to averaging}{
The tempting argument is that gridding averages several observations per
cell, so bad points wash out. Only some do. Two of the defects below are
\emph{systematic}: the opening metres of every pass read low and the closing
metres read high, and every pass starts and ends at the same two edges of the
paddock. Averaging preserves that, and it arrives in the analysis as a
spatial trend along the direction of travel, which a spatial model will
happily fit. Only the random outliers are diluted, and they are the ones that
matter least.

Whether the trend reaches the treatment estimate depends on how the passes
lie against the treatment strips. Run the passes along the strips and each
one sits inside a single treatment, so the fault falls on all of them
equally: on simulated trials of that shape, cleaning moves the recovered
contrast by about a percentage point. Run them across the strips and it does
not fall equally, and the trend becomes a treatment effect. The case for
cleaning regardless is that it costs little and the direction of harvest is
often not the analyst's to choose.
}

\section{The rules}{
Each is optional and each is off unless its argument is set, except the
outlier rules, which default to conservative values. Every removed point is
attributed to the first rule that caught it.

\describe{
\item{\code{missing}}{No yield, or no position.}
\item{\code{range}}{Outside \code{yield_range}. There is no default: the plausible
range depends on the crop and the units, and guessing it would be worse
than not applying the rule.}
\item{\code{zero}}{At or below \code{min_yield}. A zero reading while the machine is
cutting is impossible rather than extreme, and it sits well inside any
dispersion-based threshold -- which is why a statistical rule on its own
leaves dropouts in. \strong{Set this for real data}: \code{min_yield = 0} is right
for a yield in physical units. It is off by default for the same reason
\code{yield_range} is: the function cannot see the units, and a threshold in
the wrong ones removes good data. The MAD rules need no such care
because they are relative to the spread of the data itself.}
\item{\code{outlier}}{Further than \code{outlier_mad} median absolute deviations
from the median yield. The median and MAD are used rather than the mean
and standard deviation because the outliers inflate the standard
deviation, which is how a three-sigma rule ends up keeping the very
points it was meant to remove. Even so this rule only catches gross
spikes: the dispersion it measures includes the field's real variation,
so the threshold has to stay loose.}
\item{\code{local}}{Further than \code{local_mad} deviations from the median of the
point's own neighbourhood, on a grid of \code{local_cell} metres. Taking the
comparison local removes the field's own variation from it, so a much
tighter threshold is safe -- this is the rule that catches a bad point
sitting in a good part of the paddock. Off unless \code{local_mad} is set.
Use 3: on labelled simulated clouds it lifts the share of injected
outliers removed from 39\% to 58\% while costing 1\% of the good points,
and tightening it to 2 finds no more of them but removes 8\%.}
\item{\code{speed}}{Zero or negative speed, or further than \code{speed_mad} MADs
from the median. Needs a \code{speed} column.}
\item{\code{pass_end}}{Within \code{pass_trim} metres of either end of a pass, along
the direction of travel. This is the grain-flow rule, and the one worth
setting: a combine's fill time is a few seconds, which at working speed
is of the order of ten metres.}
\item{\code{edge}}{Within \code{edge_buffer} metres of the boundary of the cloud's
convex hull -- the headland, where the machine is turning, part-full, or
driving over ground it has already cut.}
}
}

\examples{
sim <- simulate_yield_monitor(defects = TRUE, seed = 1)
clean <- clean_yield_monitor(sim$cloud, pass = "pass", order = "seq",
                             speed = "speed", pass_trim = 12, local_mad = 3)
attr(clean, "cleaning")

# What was removed, and from where
audit <- clean_yield_monitor(sim$cloud, pass = "pass", order = "seq",
                             speed = "speed", pass_trim = 12, drop = FALSE)
table(audit$.reason)

}
\seealso{
\code{\link[=grid_dense_layer]{grid_dense_layer()}}, which is what this feeds, and
\code{\link[=simulate_yield_monitor]{simulate_yield_monitor()}} with \code{defects = TRUE}, which generates a cloud
whose bad points are labelled so a cleaning rule can be scored.
}
